.container.user-new.clearfix
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    .user-new-1
      = devise_error_messages!
    .row
    .col-xs-12
      .user-regist
        .field
          .text
            %span Facebook
            または
            %span Google
            で登録
        .signup-separator
          .signup-separator__text または
          %hr
        .field
          .field-input
            = f.email_field :email, placeholder: "メールアドレス", autofocus: true, autocomplete: "email", class: 'input-form'
            %span.glyphicon.glyphicon-envelope
        .field
          .field-input
            = f.text_field :last_name, placeholder: "姓", class: 'input-form'
            %span.glyphicon.glyphicon-user
        .field
          .field-input
            = f.text_field :first_name, placeholder: "名", class: 'input-form'
            %span.glyphicon.glyphicon-user
        .feild
          .field-input
            = f.password_field :password, placeholder: "パスワードを作成", autocomplete: "off", class: 'input-form'
            %span.glyphicon.glyphicon-lock
          .field-alert
        .field
          .field-input
            = f.password_field :password_confirmation, placeholder: "パスワード(確認)", autocomplete: "off", class: 'input-form'
            %span.glyphicon.glyphicon-lock
        .field
          .field-label
            = f.label :birth_day, "生年月日"
            .text ご登録は18歳以上の方に限ります。誕生日は非公開です
          .field-input
            = f.date_select(                        |
              :birth_day,                           |
              include_blank: true,                  |
              start_year:     1930,                 |
              end_year:       (Time.now.year - 18)) |
        .actions
          = f.submit "登録",class: "submit-btn"
        = render "devise/shared/links"

